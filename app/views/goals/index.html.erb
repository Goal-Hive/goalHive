<div class="w-full p-5">
  <div class="flex justify-between items-center p-3" >
    <div class="flex items-center gap-x-3 font-semibold  text-base text-light-text-title ">
      <span class="font-bold text-xl" id="goals_count"> <%= current_user.goals.count %> </span>
      <h1 class="font-bold text-2xl">Goals</h1>
    </div>
  </div>

   <div id="goals" class="min-w-full
                          flex flex-wrap justify-around items-start
                          gap-y-10 gap-x-10">
    <%= render @goals %>
  </div>
</div>
