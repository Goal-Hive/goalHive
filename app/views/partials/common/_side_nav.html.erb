<!--Side navigation bar-->
<div class="pl-4
            py-4
            bg-white
            flex flex-col justify-between  items-center gap-y-5
            w-2/12
            top-24
            h-[calc(100vh-150px)]
            ">
  <!--  Create a Goal-->
  <%= link_to '+ Create a goal',
              new_goal_path,
              data: { turbo_frame: "modal" },
              class: "w-full rounded-3xl py-1 text-center bg-light-btn text-white block font-bold " %>
  <div class="overflow-auto w-full">
    <%= render partial: 'categories/index' %>
  </div>
  <!--  Account Settings, Sign Out, Logo-->
  <div>
    <div class="flex flex-col gap-y-2 items-start w-full">
      <% if user_signed_in? %>
        <p> Change Theme </p>
        <%= link_to 'Account Settings', edit_user_registration_path %>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo: "false" } %>
      <% else %>
        <%= link_to "Log in", new_user_session_path %>
        <%= link_to "Register", new_user_registration_path %>
      <% end %>
    </div>
    <p class="font-black mt-6">GoalHive.Inc ™</p>
  </div>
</div>

