<!--Side navigation bar-->
<div class="pl-5
            py-5
            bg-white
            flex flex-col justify-between  items-center gap-y-5
            overflow-auto
            w-2/12
            top-24
            h-[calc(100vh-150px)]
            sticky
            ">
<!--  Create a Goal-->
  <%= link_to '+ Create a goal',
              new_goal_path,
              data: { turbo_frame: "modal" },
              class: "w-full rounded-3xl py-1 text-center bg-light-btn text-white block font-bold " %>

<!--  Add Category, Categories-->
  <div class="bg-light-sideNav h-full w-full p-3 sticky top-0">
    <div class="flex flex-col justify-between w-full">
<!--      Add a category-->
      <div class="sticky flex justify-between">
        <p>Add a Category</p>
        <p class="border border-solid border-gray-200 h-5 w-5">+</p>
      </div>
<!--      Categories-->
      <div>
<!--        TODO Fix the layout when there is many categories-->
      </div>
    </div>
  </div>

  <!--  Account Settings, Sign Out, Logo-->
  <div>
    <div class="flex flex-col gap-y-2 items-start w-full">
      <% if user_signed_in? %>
        <p> Change Theme </p>
        <%= link_to 'Account Settings', edit_user_registration_path %>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, data: { turbo: "false" } %>
      <% else %>
        <%= link_to "Log in", new_user_session_path %>
        <%= link_to "Register", new_user_registration_path %>
      <% end %>
    </div>
    <p class="font-black mt-6">GoalHive.Inc ™</p>
  </div>
</div>

