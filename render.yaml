databases:
  - name: goalHive
    databaseName: goal_hive
    user: goal_hive

services:
  - type: web
    name: goalHive
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: goal_hive
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false